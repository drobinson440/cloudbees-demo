pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo "${test_message}"
                //publishEvent simpleEvent('helloWorld')
            }
        }
    }
}

